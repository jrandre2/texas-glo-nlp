# Legacy Outputs

This folder stores imported artifacts from external/older repositories.

Current namespace:

- `capacity_sem_migrated/`
  - generated by `scripts/import_capacity_sem_legacy.py`
  - contains deduplicated files + `manifest.json` provenance

This legacy namespace is consumed by `make sem-compare` to benchmark the latest
Texas SEM estimation outputs against migrated historical model results.
